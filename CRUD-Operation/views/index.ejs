<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/styles.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="bg-zinc-900 flex items-center justify-center min-h-screen">
      <form
        class="bg-zinc-800 text-white p-6 rounded-xl shadow-lg w-full max-w-md space-y-4" action="/create" method="post"
      >
        <h2 class="text-2xl font-bold text-center">Create User</h2>

        <div>
          <label class="block text-sm mb-1" for="name">Name</label>
          <input
            type="text"
            id="name"
            name="name"
            placeholder="Enter name"
            class="w-full bg-zinc-800 text-white border border-zinc-700 p-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>

        <div>
          <label class="block text-sm mb-1" for="email">Email</label>
          <input
            type="email"
            id="email"
            name="email"
            placeholder="Enter email"
            class="w-full bg-zinc-800 text-white border border-zinc-700 p-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>

        <div>
          <label class="block text-sm mb-1" for="username">Image URL</label>
          <input
            type="text"
            id="image-url"
            name="image"
            placeholder="Enter Image URL"
            class="w-full bg-zinc-800 text-white border border-zinc-700 p-2 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>

        <button
          type="submit"
          class="w-full bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 rounded-lg transition-all"
        >
          Create User
        </button>

        <!-- Read Users Link -->
        <a
          href="/read"
          class="w-full block text-center bg-indigo-600 hover:bg-indigo-700 text-white font-semibold py-2 rounded-lg transition-all"
        >
          Read Users
        </a>
      </form>
    </div>
  </body>
</html>
